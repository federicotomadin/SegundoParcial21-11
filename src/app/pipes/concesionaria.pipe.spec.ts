import { ConcesionariaPipe } from './concesionaria.pipe';

describe('ConcesionariaPipe', () => {
  it('create an instance', () => {
    const pipe = new ConcesionariaPipe();
    expect(pipe).toBeTruthy();
  });
});
